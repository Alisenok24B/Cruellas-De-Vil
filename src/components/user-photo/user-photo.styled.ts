import styled from '@emotion/styled';

export const StyledUserPhoto = styled.img`
    max-width: 270px;
    width: 30vw;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    object-position: center;
`;